package com.localizedjpa.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main Spring Boot application for Localized JPA Demo.
 */
@SpringBootApplication
public class LocalizedJpaDemoApplication {

    public static void main(String[] args) {
        SpringApplication.run(LocalizedJpaDemoApplication.class, args);
    }
}
